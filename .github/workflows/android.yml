name: Build and test Android APK

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SIGNING_KEYSTORE_PATH: /tmp/daily-wisdom-release.jks
      SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
      SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
      SIGNING_KEY_ALIAS: daily-wisdom
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
      - name: Validate release version
        if: startsWith(github.ref, 'refs/tags/')
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: python3 scripts/prepare_release.py --tag "$RELEASE_TAG"
      - name: Validate extracted quotes
        run: python3 scripts/check_quotes.py
      - name: Restore fixed signing key
        env:
          SIGNING_KEYSTORE_BASE64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
        run: python3 scripts/restore_signing.py
      - name: Test, lint and build signed release APK
        run: ./gradlew --no-daemon testDebugUnitTest lintRelease assembleRelease
      - name: Verify APK signature and package
        run: |
          python3 scripts/verify_apk.py app/build/outputs/apk/release/app-release.apk
          mkdir -p dist
          cp app/build/outputs/apk/release/app-release.apk dist/daily-wisdom.apk
          cd dist && sha256sum daily-wisdom.apk > SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: daily-wisdom-apk
          path: dist/
          if-no-files-found: error
          retention-days: 90
      - name: Remove temporary signing key
        if: always()
        run: python3 -c "import os; from pathlib import Path; Path(os.environ['SIGNING_KEYSTORE_PATH']).unlink(missing_ok=True)"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-test-reports
          path: app/build/reports/

  device-smoke-test:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      SIGNING_KEYSTORE_PATH: /tmp/daily-wisdom-release.jks
      SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
      SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
      SIGNING_KEY_ALIAS: daily-wisdom
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
      - uses: actions/download-artifact@v4
        with:
          name: daily-wisdom-apk
          path: ci-release
      - name: Restore fixed signing key
        env:
          SIGNING_KEYSTORE_BASE64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
        run: python3 scripts/restore_signing.py
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          target: google_apis
          profile: pixel_2
          disable-animations: true
          script: |
            ./gradlew --no-daemon connectedDebugAndroidTest
            adb pull /data/local/tmp/widget-short.png app/build/reports/widget-short.png
            adb pull /data/local/tmp/widget-long.png app/build/reports/widget-long.png
            adb pull /data/local/tmp/favorites.png app/build/reports/favorites.png
            adb pull /data/local/tmp/widget-wrap.png app/build/reports/widget-wrap.png
            adb pull /data/local/tmp/widget-theme-0.png app/build/reports/widget-theme-0.png
            adb pull /data/local/tmp/widget-theme-1.png app/build/reports/widget-theme-1.png
            adb pull /data/local/tmp/widget-theme-2.png app/build/reports/widget-theme-2.png
            adb pull /data/local/tmp/widget-square.png app/build/reports/widget-square.png
            adb pull /data/local/tmp/widget-strip.png app/build/reports/widget-strip.png
            adb pull /data/local/tmp/widget-accessible.png app/build/reports/widget-accessible.png
            ./gradlew --no-daemon assembleRelease -PappVersionCode=1
            python3 scripts/verify_apk.py app/build/outputs/apk/release/app-release.apk
            adb install app/build/outputs/apk/release/app-release.apk
            adb shell am start -W -n com.sinter.daily/.MainActivity
            adb install -r ci-release/daily-wisdom.apk
            adb shell am start -W -n com.sinter.daily/.MainActivity
            adb shell input keyevent 82
            adb shell screencap -p /sdcard/daily-wisdom.png
            adb pull /sdcard/daily-wisdom.png app/build/reports/daily-wisdom.png
      - name: Remove temporary signing key
        if: always()
        run: python3 -c "import os; from pathlib import Path; Path(os.environ['SIGNING_KEYSTORE_PATH']).unlink(missing_ok=True)"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: android-device-test-reports
          path: app/build/reports/

  publish-release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build, device-smoke-test]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: daily-wisdom-apk
          path: ci-apk
      - name: Prepare versioned release assets
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: python3 scripts/prepare_release.py --tag "$RELEASE_TAG" --artifact-dir ci-apk
      - name: Publish GitHub Release with APK
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          if draft=$(gh release view "$RELEASE_TAG" --json isDraft --jq .isDraft 2>/dev/null); then
            if [ "$draft" = "false" ]; then
              echo "Release already published; preserving its existing assets."
              exit 0
            fi
          else
            gh release create "$RELEASE_TAG" --verify-tag --draft \
              --title "一日一句 $RELEASE_TAG" --notes-file release-notes.md
          fi
          gh release upload "$RELEASE_TAG" release-assets/* --clobber
          gh release edit "$RELEASE_TAG" --draft=false --notes-file release-notes.md
          gh release view "$RELEASE_TAG" --json url,assets
